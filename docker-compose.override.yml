# Dev override (applied automatically by `docker compose`).
# Runs ha_ingest from the live source tree so edits don't require a rebuild.

services:
  ha_ingest:
    build: .
    volumes:
      - ./src:/app/src
    environment:
      LOGURU_LEVEL: DEBUG
